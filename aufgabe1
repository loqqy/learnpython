#menu code

x = input('Choose between Menu 1 or menu 2 or 0 to return to main menu') #display menu decisions
while x == '0':                                                          #0 entered, stay or return to main menu
	print 'restart the file to chose from either 1 or 2'                   #and display the initial choice
		if x == '1'                                                          #if 1 chosen, call menu one app
			elif x == '2'                                                      #if 2 is chosen, call menu two app
				else print 'Invalid number'                                      #every other number displays "invalid number"


# menu 1
def fib(n)
a, b = 0, 1
	while b < n:
		print b
		a, b = b, a+b

print fib(100)

#menu 2

print 'this message will be flipped'[::-1]
